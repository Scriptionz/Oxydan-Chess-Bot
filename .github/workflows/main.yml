name: Oxydan Lichess Bot

on:
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest
    steps:
      - name: Kodlari Cek (LFS Destekli)
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: LFS Dosyalarini Tamamen Indir
        run: git lfs pull

      - name: Kitap Dosyasini Dogrula (Debug)
        run: |
          echo "--- DOSYA KONTROLU ---"
          ls -lh ./M11.2.bin
          # Eğer dosya 1KB ise LFS hatası var demektir. 232MB olmalı.

      - name: Python Kurulumu
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Motoru Derle
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential clang
          cd src
          # -march=native yerine uyumluluk için standard x86-64 seçilebilir ama kalsın
          clang -O3 -std=gnu11 -Wall -Wextra -DNDEBUG -flto -DUSE_NNUE=0 -march=native *.c pyrrhic/tbprobe.c -lpthread -lm -o Ethereal
          chmod +x Ethereal
          echo "Motor basariyla derlendi: $(ls -F Ethereal)"
          cd ..

      - name: Kutuphaneleri Kur
        run: |
          pip install -r requirements.txt

      - name: Oxydan Self-Check (Mantıksal Denetim)
        run: |
          echo "🤖 Botun beyin fonksiyonları ve motor uyumu test ediliyor..."
          python check_bot.py

      # Bu adım aslında Self-Check başarılıysa çalışır, ama garantiye alalım
      - name: Motor Test (UCI Testi)
        if: success()
        run: |
          chmod +x ./src/Ethereal
          echo "uci" | ./src/Ethereal | grep "uciok" || (echo "MOTOR CALISMIYOR!" && exit 1)
          echo "Motor UCI testinden gecti."

      - name: Botu Baslat
        if: success()
        env:
          LICHESS_TOKEN: ${{ secrets.LICHESS_TOKEN }}
        run: |
          # Token yerleştirme (config.yml dosyanın ana dizinde olduğunu varsayıyoruz)
          sed -i "s/\$LICHESS_TOKEN/$LICHESS_TOKEN/g" config.yml
          
          for i in {1..10}; do
            echo "Oxydan v8.0 Deneme $i/10..."
            # Dosya adının tam doğru olduğundan emin ol (lichess-bot.py)
            python -u lichess-bot.py && break || echo "Bot kapandi, 5sn sonra tekrar..."
            sleep 5
          done
